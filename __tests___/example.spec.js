import React from 'react';
import { mount, shallow } from 'enzyme';
import exampleComponent from '../client/example-component/example-component.js';

describe('carousel component', () => {
  
  it('is a test test case', () => {
    expect(exampleComponent.exampleString).toEqual('a string');
  });

});
